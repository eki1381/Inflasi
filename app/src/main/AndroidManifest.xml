<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eki.ryh.inflasi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.eki.ryh.inflasi.Main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.eki.ryh.inflasi.Questionnaire.QuestionnaireActivity"></activity>

        <activity android:name="com.eki.ryh.inflasi.Barang.AddBarangActivity"></activity>

        <activity android:name="com.eki.ryh.inflasi.Merek.AddMerekActivity"></activity>

    </application>
</manifest>
